deploy:
  provider: heroku
  api_key: _w#5(y!bx$u9kr2a8rg!r3!l_+o92&1p(y*bc599kf^wg*0mdn
  app: hooshome

language: python
python: 
  - "3.6.1" 
install:
  - pip install -r requirements-build.txt
  - pip install social-auth-app-django
# command to run tests
script:
  - python manage.py test home
